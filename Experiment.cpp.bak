#include <bits/stdc++.h>

using namespace std;

int main() {
    vector<int> v;

    v.push_back(1);
    v.push_back(1);
    v.push_back(1);
    v.push_back(1);
    v.push_back(1);

    v.insert(v.begin()+1, 2);

    for (int i = 0; i < v.size(); i++) {
        cout << v[i] << " ";
    }
    cout << endl;

    v.erase(v.begin()+3);

    for (int i = 0; i < v.size(); i++) {
        cout << v[i] << " ";
    }
    cout << endl;

    string str = "";
    str += "abc";

    cout << str << endl;


    priority_queue<int> pq;
    return 0;
}
